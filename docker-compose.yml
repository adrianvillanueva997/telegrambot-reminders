services:
  bot:
    image: ghcr.io/adrianvillanueva997/telegrambot-reminders:v1.0.7
    container_name: telegrambot_reminders
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
